# Build outputs
build*/
.deps/
.deps-vs/

# Multica local cache (issue descriptions, agent instructions, helpers) — see docs/multica-archive.tar.gz for snapshot
.multica/
# Multica issue description files (派活单原文) — packed into docs/multica-archive.tar.gz
.hermes/issues/
# Multica monitor scripts (legacy) — packed into docs/multica-archive.tar.gz
.hermes/issues/*.sh
.hermes/plans/
.hermes/triage/

# Multica tarball extraction byproducts (tarfile.extractall leakage — already a tar.gz at docs/multica-archive.tar.gz)
/docs/multica-archive/

# Developer handoff doc (not for public release)
/WVmp_HANDOVER_*.md

# Stray Windows redirect artifacts
/NUL
/wvmp_runtime_dump.bin

# IDE / editor state
.vs/
.vscode/
.idea/
*.user
CMakeUserPresets.json

# Compiler / test artifacts
*.obj
*.lib
*.pdb

# WVmp repo hygiene additions (MIT-382, 2026-08-27)
.multica_tmp/
multica_workspaces/
/wvmp.slnx
scripts/__pycache__/
/test_output.txt
/build_*.log
/ctest_*.log
/native_*.txt
/protected_*.txt
/cl_shift_e2e.txt
/popcnt_test.asm
Testing/
